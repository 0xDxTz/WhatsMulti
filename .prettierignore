dist
lib
coverage
node_modules
bun.lock

# v1 source, kept for reference during the rewrite. Removed in phase 11.
legacy
# v1 documentation, still describing the v1 API. Rewritten in phase 11.
README.md

landing

# Generated. Formatting is owned by the generator so `gen:check` stays stable.
src/generated
spec/vectors
