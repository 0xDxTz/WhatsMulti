{
    // The examples are compiled, not just displayed: an example that no longer type
    // checks is a documentation bug, and this is what makes CI say so.
    //
    // `paths` points the package name at src/ so the examples read exactly as a
    // consumer's code would, without needing a build first.
    "extends": "../tsconfig.json",
    "compilerOptions": {
        "noEmit": true,
        "rootDir": "..",
        "baseUrl": ".",
        "paths": {
            "whatsmulti": ["../src/index.ts"],
            "whatsmulti/qr": ["../src/qr/index.ts"],
            "whatsmulti/mongo": ["../src/adapters/mongo/index.ts"],
            "whatsmulti/redis": ["../src/adapters/redis/index.ts"],
            "whatsmulti/sql": ["../src/adapters/sql/index.ts"],
            "whatsmulti/webhook": ["../src/webhook/index.ts"],
            "whatsmulti/server": ["../src/server/index.ts"]
        }
    },
    "include": ["**/*.ts"]
}
